{
  "name": "jest-test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "test": "jest",
    "pw": "npx playwright test playwright.test.ts",
    "update:pw": "npx playwright test playwright.test.ts --update-snapshots",
    "report:pw": "npx playwright show-report",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "~5.6.2",
    "vite": "^6.0.5"
  },
  "dependencies": {
    "playwright": "^1.52.0"
  }
}
